name: transfer_path_service
description: Dart service for calculating optimal credit card point transfer paths.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  postgres: ^3.0.0 # For connecting to Cloud SQL
